# Comandos

```

Options:
      --help      Show help                                            [boolean]
      --version   Show version number                                  [boolean]
  -b, --base      Base de la tabla de multiplicar            [number] [required]
  -n, --numlimit  Número de impresiones límite de la tabla en la consola y el
                  archivo de texto                        [number] [default: 10]
  -l, --listar    Opción de mostrar la tabla en la consola             [boolean]

```